apiVersion: v1
kind: Service
metadata:
  name: query-api
  labels:
    app: query-api
    component: query
spec:
  type: ClusterIP
  selector:
    app: query-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
